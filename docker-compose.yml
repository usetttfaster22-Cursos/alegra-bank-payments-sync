services:
  app:
    build: .
    container_name: alegra-bank-payments-sync
    restart: unless-stopped
    ports:
      - "127.0.0.1:${HOST_PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
